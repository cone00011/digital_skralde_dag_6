---
import { useState } from 'react';

let formData = {
  firstName: '',
  lastName: '',
  email: '',
  dataDeleted: 0,
  termsAccepted: false,
  newsletter: false
};

function handleSubmit(event) {
  event.preventDefault();
  console.log('Form submitted:', formData);
}
---
<div class="relative flex items-center justify-center min-h-screen bg-white">
    <!-- Grid baggrund -->
    <div class="absolute inset-0 bg-[linear-gradient(to_right,rgba(0,0,0,0.2)_1px,transparent_1px),linear-gradient(to_bottom,rgba(0,0,0,0.2)_1px,transparent_1px)] bg-[size:80px_80px] pointer-events-none"></div>
  
    <!-- Formular -->
    <form id="form" class="relative z-10 bg-white p-8 border border-[#1100FF] shadow-lg max-w-md w-full">
      <h2 class="text-[#1100FF] text-[20px] md:text-[32px] text-center mb-4">
        REGISTRER DIT BIDRAG
      </h2>
      <p class="text-gray-600 text-center mb-6 text-sm md:text-lg">
        VENLIGST UDFYLD OPLYSNINGERNE NEDENFOR.
      </p>
  
      <label for="fornavn">Fornavn</label>
      <input type="text" id="fornavn" name="fornavn" placeholder="FORNAVN" required
        class="w-full p-4 mb-4 border border-[#1100FF] bg-white focus:ring focus:ring-[#1100FF] outline-none" />
    
      <label for="efternavn">Efternavn</label>
      <input type="text" id="efternavn" name="efternavn" placeholder="EFTERNAVN" required
        class="w-full p-4 mb-4 border border-[#1100FF] bg-white focus:ring focus:ring-[#1100FF] outline-none" />
    
      <label for="email">Email</label>
      <input type="email" id="email" name="email" placeholder="EMAIL" required
        class="w-full p-4 mb-4 border border-[#1100FF] bg-white focus:ring focus:ring-[#1100FF] outline-none" />
    
      <label for="maengde">Mængde data slettet</label>
      <div class="mb-6 relative">
        <input type="number" id="maengde" name="maengde" min="0" placeholder="MÆNGDE DATA SLETTET"
          class="w-full p-4 pr-12 border border-[#1100FF] bg-white focus:ring focus:ring-[#1100FF] outline-none" />
        <span class="absolute right-4 top-1/2 transform -translate-y-1/2 text-[#1100FF]">GB</span>
      </div>
      
      <div class="mb-6">
        <label class="flex items-center space-x-2">
          <input type="checkbox" required
            class="w-5 h-5 border-2 border-[#1100FF] focus:ring-[#1100FF] checked:bg-[#1100FF]" />
          <span>Jeg accepterer betingelserne.</span>
        </label>
        <label class="flex items-center space-x-2 mt-4">
          <input type="checkbox"
            class="w-5 h-5 border-2 border-[#1100FF] focus:ring-[#1100FF] checked:bg-[#1100FF]" />
          <span>Jeg vil gerne tilmelde mig nyhedsbrevet.</span>
        </label>
      </div>
  
      <button type="submit"
        class="w-full bg-white text-[#1100FF] p-4 border border-[#1100FF] transition hover:bg-[#1100FF] hover:text-white">
        REGISTRER
      </button>
    </form>
  </div>
  